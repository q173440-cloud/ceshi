# 校园网自动登录项目 - 会话摘要

## 项目路径
`E:\ceshi`

## 已有文件
- `auto_login.ps1` — 校园网自动登录脚本（PowerShell）
- `config.json` — 配置文件（账号/密码/运营商）
- `install_scheduler.bat` — 开机自启安装
- `uninstall_scheduler.bat` — 卸载

## 问题现状
脚本测试不通过，目前诊断出两个问题：

### 问题1：登录 URL 错误（已修复）
- **旧（错的）**：`http://10.2.255.26/eportal/InterFace.do?method=login`
- **新（已改）**：`http://10.2.255.26/eportal/?c=ACSetting&a=Login`
- 校园网系统：安徽新华学院 Dr.COM 哆点 4.0

### 问题2：配置信息为占位数据
`config.json` 中的账号 17344086087 / 密码 888888 是随便填的测试数据，服务器返回 `userid error1`。

### 运营商配置
```json
1 → 校园用户（无后缀）
2 → 校园电信（@dx）
3 → 校园联通（@lt）
4 → 校园其他（无后缀）
```

## 下一步需要做的事
1. 需要获取**真实的校园网账号密码**
2. 需要 F12 抓包确认 POST 请求的精确格式（URL、参数、请求头）
3. 验证登录成功后自动保存配置

## 代理切换命令（bash）
已配置在 `.bashrc` 中：
- `dl_1` — 开启代理 127.0.0.1:10809
- `dl_0` — 关闭代理直连
